language: python
python:
  - 3.8
install:
  - pip install -U tox-travis
  - pip install -U setuptools && pip install tox tox-travis coveralls
script:
  - tox < testing.txt
after_success:
  - coveralls